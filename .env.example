# Create a bot via @BotFather on Telegram to get the token.
TELEGRAM_BOT_TOKEN=

# Message your bot once, then GET https://api.telegram.org/bot<token>/getUpdates
# and read the chat id back from the response.
TELEGRAM_CHAT_ID=
